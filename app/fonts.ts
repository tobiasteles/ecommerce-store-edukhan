// app/fonts.ts
import { Kode_Mono } from 'next/font/google';

export const kodeMono = Kode_Mono({
  subsets: ['latin'],
  weight: ['400', '700'], // Pode ajustar conforme necessário
  display: 'swap',
});